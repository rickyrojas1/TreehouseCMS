'use strict';

/**
 * staff-pick-submission router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::staff-pick-submission.staff-pick-submission');
