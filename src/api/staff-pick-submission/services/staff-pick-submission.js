'use strict';

/**
 * staff-pick-submission service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::staff-pick-submission.staff-pick-submission');
