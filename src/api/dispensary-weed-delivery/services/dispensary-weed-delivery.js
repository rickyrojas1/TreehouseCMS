'use strict';

/**
 * dispensary-weed-delivery service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::dispensary-weed-delivery.dispensary-weed-delivery');
